# Core
fastapi
uvicorn[standard]
python-dotenv

# LLM / routing
httpx           # Ollama API calls

# Input layer
Pillow          # EXIF extraction (shared with farming)
python-docx     # Document processing
pypdf2          # PDF processing
openai-whisper  # Local audio transcription

# Knowledge graph / people
# insightface   # Face recognition — uncomment when building people graph

# Web search (privacy proxy)
# searxng is self-hosted — no Python package needed

# Dashboard
matplotlib      # Weather chart generation
numpy           # Array ops for chart data

# Storage
# SQLite is stdlib — no extra package needed
